import { instance } from "./service";

// setTimeout(() => {
//   instance.sendData({ name: 'Den', age: '29' });
// }, 500)

instance.sendData({ name: 'Den', age: '29' });
instance.sendData({ name: 'Jhon', age: '25' });